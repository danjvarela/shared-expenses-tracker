# App environment: development | production | demo (defaults to development)
APP_ENV=development

# Drizzle
DATABASE_URL=file:local.db

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Ops: shared secret required by POST /gc/receipts. App fails to boot if unset.
GC_SECRET=
